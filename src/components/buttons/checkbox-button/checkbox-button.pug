mixin checkbox-button(name, caption, checked)        
    label.chebox-button(class!=attributes.class) !{caption}
        if checked
            input.checkbox-button__input(type='checkbox' name= name checked)
        else
            input.checkbox-button__input(type='checkbox' name= name)
        span.checkbox-button__checkmark